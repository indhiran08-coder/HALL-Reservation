# Nixpacks build configuration for Railway
# Tells Railway how to build and run the Spring Boot backend

[phases.setup]
nixPkgs = ["jdk21"]

[phases.build]
cmds = ["mvn clean package -DskipTests -q"]

[start]
cmd = "java -jar target/hall-reservation-1.0.0.jar"
